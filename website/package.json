{
  "name": "riina-website",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "bash ../scripts/generate-metrics.sh && (bash ../scripts/build-wasm.sh || true)",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bash ../scripts/deploy-website.sh"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "gh-pages": "^6.3.0"
  }
}
